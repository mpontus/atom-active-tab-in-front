'use babel';

import ActiveTabInFrontView from '../lib/active-tab-in-front-view';

describe('ActiveTabInFrontView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
